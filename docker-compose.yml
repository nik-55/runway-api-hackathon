services:
  app:
    build: .
    container_name: reelagent
    env_file: .env
    volumes:
      - ./cookies_yt.txt:/app/cookies_yt.txt:ro
      - ./media:/app/media
      - ./logs:/app/logs
      - ./data:/app/data
    ports:
      - "8000:8000"
